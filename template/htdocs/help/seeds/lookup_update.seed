	
	var msg='';
	var #lookupkey#=o#lookupkey#.value2;
	if (!#lookupkey#&&!o#lookupkey#.disabled){
		msg="pick a #record# from the list\n";
		valid=0;
		offender=offender||o#lookupkey#;
	}
	
	if (msg!='') alert(msg);
	
	if (!#lookupkey#) #lookupkey#=0;

	